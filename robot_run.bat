robot --timestamp -d Results -X VLMS5.0_Regression

# robot --timestamp -d Results -i sample VLMS5.0_Regression

# robot --timestamp -d Results -e sample VLMS5.0_Regression

# robot --timestamp -d Results -b VLMS5.0_Regression

# Usage:
# -b is for debugging the specified file, -X is for exit the complete execution on test case failure, -d is for storing the reports in a designated dictionary,
# -i is for include/run specified tags, -e is for exclude specified tags, --timestamp is for storing all report files.
